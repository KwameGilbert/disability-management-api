const express=require("express"),app=express(),cors=require("cors"),morgan=require("morgan"),routes=require("./routes");app.use(cors()),app.use(express.json()),app.use(morgan("dev")),app.use("/api",routes),module.exports=app;